# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# Node
node_modules/

# Large / runtime artifacts (not source)
.cache/
chroma_db/
tmp/
data/
output/
artifacts/
*.sqlite
*.sqlite3
*.db

# Logs & OS
*.log
.DS_Store

# Secrets. .env held a live OPENROUTER_API_KEY, AWS_BEARER_TOKEN_BEDROCK,
# VLLM_API_KEY and OMOP_DB_PASS while tracked. Committed once in 6ffd07c; those
# values are still reachable in history, so rotate them before this repo gains a
# remote. Keep the file on disk, out of the index.
.env
.env.runtime
.env.local
secrets/
# Per-session tool state, not project config. artemis/.moai holds only
# state/session-memo.md keyed to a session id; the tracked SPECs live at the
# Broadsea root, not here.
.moai/
