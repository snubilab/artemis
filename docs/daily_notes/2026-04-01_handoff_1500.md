# 2026-04-01 Handoff (15:00 KST)

## TL;DR

Gold vs AI HR 비교를 제대로 하려면 **PLATO gold-only 362명에게 outcome 이벤트 주입이 필요**.
LEADER gold-only 837명은 229명이 이미 이벤트 있음 (그래도 AI cohort 기준 주입이라 왜곡됨).

---

## 현재 실행 중인 프로세스

| 프로세스 | 위치 | 로그 | 상태 |
|---------|------|------|------|
| Agent2 shard0 | artemis-api container | `/tmp/bench_agent2_shard0.log` | ~5-6% (110/1987) |
| Agent2 shard1 | artemis-api container | `/tmp/bench_agent2_shard1.log` | ~5-6% |
| Agent2 shard2 | artemis-api container | `/tmp/bench_agent2_shard2.log` | ~5-6% |
| Agent2 shard3 | artemis-api container | `/tmp/bench_agent2_shard3.log` | ~5-6% |
| LLM gpt-4o | host PID 34478 | `/private/tmp/bench_llm_gpt_4o_full.log` | ~65% |
| LLM gpt-4.1 | host PID 34481 | `/private/tmp/bench_llm_gpt_4_1_full.log` | ~90% |
| LLM gpt-5-chat | host PID 34487 | `/private/tmp/bench_llm_gpt_5_chat_full.log` | ✅ 완료 |
| LLM gpt-5.1 | host PID 34492 | `/private/tmp/bench_llm_gpt_5_1_full.log` | ~65% |
| LLM gpt-5.2 | host PID 34499 | `/private/tmp/bench_llm_gpt_5_2_full.log` | ~65% |
| LLM gpt-5.4 | host PID 34500 | `/private/tmp/bench_llm_gpt_5_4_full.log` | ~41% |
| RAG rerun | artemis-api container | `/tmp/bench_rag_rerun.log` | ~1% (ETA 새벽) |
| ARISTOTLE gold | WebAPI cohort 1138 | - | RUNNING (60min timeout) |

---

## 모니터링 커맨드

```bash
# Agent2 shards
watch -n 10 'for s in 0 1 2 3; do echo -n "shard$s: "; docker exec artemis-api grep -oE "Agent2 [0-9]+/[0-9]+" /tmp/bench_agent2_shard${s}.log 2>/dev/null | tail -1; done'

# LLM 진행
for model in gpt_4o gpt_4_1 gpt_5_chat gpt_5_1 gpt_5_2 gpt_5_4; do echo -n "$model: "; grep -oE "LLM [0-9]+/[0-9]+" /private/tmp/bench_llm_${model}_full.log | tail -1; done

# ARISTOTLE gold
curl -s "http://127.0.0.1/WebAPI/cohortdefinition/1138/info" | python3 -c "import sys,json,re; raw=re.sub(r'[\x00-\x1f]',' ',sys.stdin.read()); d=json.loads(raw); [print(g['status'],g.get('personCount')) for g in d]"
```

---

## Gold vs AI HR 현재 결과

```
결과 파일: /tmp/gold_vs_ai_results.json
```

| Study | Gold N | AI N | Overlap | Recall | Gold HR | AI HR | Published HR |
|-------|--------|------|---------|--------|---------|-------|-------------|
| LEADER | 1,222 | 387 | 385 | 31.5% | 3.976 [3.20-4.94] | 16.348 [11.66-22.93] | 0.87 [0.78-0.97] |
| PLATO | 436 | 75 | 74 | 17.0% | **1.000 [1.00-1.00] ❌** | 0.973 [0.94-1.01] | 0.84 [0.77-0.92] |
| ARISTOTLE | 0 | 400 | 0 | N/A | N/A | 1.793 [1.45-2.21] | 0.79 [0.66-0.95] |

### PLATO Gold HR=1.0 원인

CV outcome 이벤트가 AI cohort(75명) 환자에게만 주입됨.
gold-only 362명은 이벤트 0 → IPTW HR이 1.0으로 collapse됨.

### LEADER Gold HR=3.97 원인

gold-only 837명 중 229명은 이미 이벤트 있음(Synthea 기본 데이터).
나머지 608명은 이벤트 없음 → HR 왜곡.

---

## 다음 해야 할 일

### 1. PLATO/LEADER gold 환자에게 outcome 이벤트 주입 (최우선)

**현황:**
- `synthea_cdm_leader.condition_occurrence` 백업: `condition_occurrence_backup_20260401` ✅
- `synthea_cdm_plato.condition_occurrence` 백업: `condition_occurrence_backup_20260401` ✅

**주입 방법:**
PLATO의 경우 gold-only 362명에게 AI cohort와 동일한 비율(outcome concept_id 기반)로 이벤트 주입.
참고: 기존 주입은 AI cohort 75명 기준 20% treatment rate로 진행됨.

주입 후 WebAPI에서 outcome cohort 재생성 필요:
- LEADER outcome: cohort_id=841
- PLATO outcome: cohort_id=943

주입 후 `run_gold_vs_ai_comparison.py` 재실행.

### 2. ARISTOTLE gold cohort 완료 후 분석

WebAPI cohort 1138 RUNNING 중. 완료 확인 후:
```bash
curl -s "http://127.0.0.1/WebAPI/cohortdefinition/1138/info" | python3 -c "import sys,json,re; raw=re.sub(r'[\x00-\x1f]',' ',sys.stdin.read()); d=json.loads(raw); [print(g['status'],g.get('personCount')) for g in d]"
```
완료되면 `run_gold_vs_ai_comparison.py` 재실행.

### 3. Agent2 shard 완료 후 merge

4개 JSON 파일 merge:
```bash
python3 -c "
import json
shards = []
for s in range(4):
    with open(f'/tmp/bench_agent2_shard{s}.json') as f:
        shards.append(json.load(f))
# aggregate 합산 후 저장
"
```
결과: `artemis/data/benchmark_results/bench_agent2_full_merged.json`

### 4. LLM 완료 후 최종 비교표 작성

결과 파일: `artemis/data/benchmark_results/bench_llm_{model}_full_20260401_103722.json`

`artemis/docs/daily_notes/2026-04-01_benchmark_final_results.md` 작성.

### 5. UI 상에서 재현

study id 424(ARISTOTLE), 431(LEADER), 432(PLATO) — `http://127.0.0.1` Atlas UI에서 결과 확인.

---

## 백업 현황

| 테이블 | 백업명 | 행수 |
|--------|--------|------|
| synthea_cdm_leader.condition_occurrence | condition_occurrence_backup_20260401 | 6,063 |
| synthea_cdm_plato.condition_occurrence | condition_occurrence_backup_20260401 | 13,871 |

---

## 주요 참고 파일

- Gold vs AI 비교 스크립트: `artemis/scripts/run_gold_vs_ai_comparison.py`
- Published HR: LEADER=0.87, PLATO=0.84, ARISTOTLE=0.79
- AI study IDs: LEADER=431, ARISTOTLE=424, PLATO=432
- AI treatment cohort IDs: LEADER=840, PLATO=941, ARISTOTLE=1127
- Gold cohort IDs: LEADER=1136, PLATO=1137, ARISTOTLE=1138
- Outcome cohort IDs: LEADER=841, PLATO=943, ARISTOTLE=1128
