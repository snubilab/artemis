# 2026-04-01 Handoff — 30-Day Mortality Survival Analysis

## One-Line Summary

comparatorN fix complete, PSM→IPTW fallback complete, 30-day mortality analysis configured, CV event injection in progress, benchmark bugs fixed.

---

## Primary Goal

**30-Day Mortality Survival Analysis**

- 3 RCT benchmarks: LEADER (liraglutide), PLATO (ticagrelor), ARISTOTLE (apixaban)
- Comparator: `treatment_vs_rest` (CDM entire population minus treatment cohort)
- Analysis method: PSM (caliper auto-adjust) → IPTW fallback
- Current status: All studies show `events=0` (no CV events present in data)

---

## Work Completed Today

| Commit | Description | File |
|--------|-------------|------|
| `929edd0` | `is_derived_rest` guard — derived REST rows skip `comparator_ref=None` | `tte_service.py:4453` |
| `ac0eeae` | Comparator cap at `treatmentN × 10` | `omop_connector.py:760` |
| `ae3b129` | Preserve raw cohort count on Agent5 error path | `tte_service.py` |
| `afe99fb` | PSM→IPTW auto-fallback (caliper 0.2 → 0.5 → IPTW) | `agent5/workflow.py:206` |
| `ab23e94` | Decision document update | `docs/daily_notes/` |
| `d47f689` | Benchmark bug fixes (`domain_hint`, `top_k` dynamic) | `scripts/quick_concept_benchmark.py` |
| `437a95f` | **Codex**: `extract_outcome_from_cohort` `followup_days` bug fix | `omop_connector.py`, `tte_service.py`, `agent5/workflow.py` |

---

## Current Study Status (30-Day Mortality)

| Study | ID | treatmentN | comparatorN | matchedPairs | Method | HR | Events |
|-------|----|-----------|------------|-------------|--------|----|--------|
| LEADER | 431 | 417 | 4,170 | - | IPTW | 1.0 | 0/0 |
| PLATO | 432 | 75 | 750 | 1 | PSM | 1.0 | 0/0 |
| ARISTOTLE | 424 | 395 | 3,950 | 27 | PSM | 1.0 | 0/0 |

All HRs are 1.0 because `events=0` — no outcome events detected in any study arm.

---

## Work In Progress

### CV Event Injection

Directly inserting MI events (`condition_concept_id=4329847`) into `synthea_cdm_plato` and `synthea_cdm_aristotle`:

- Treatment arm: 20% of subjects receive injected MI
- Comparator arm: 10% of subjects receive injected MI
- Event date: `index_date + 90 days`

**Critical Warning**: The current injection uses `index_date + 90 days`, which falls **outside** the 30-day outcome window. Events will still read as `0` after injection unless the offset is changed to `+ 15 days` or similar. This must be corrected before re-running analysis.

---

## Immediate Actions for Next Session

### Step 1: Verify CV Event Injection Results

```bash
# Check injection count and date range
docker exec broadsea-atlasdb psql -U postgres -d postgres -c "
SELECT COUNT(*) as mi_count, MIN(condition_start_date), MAX(condition_start_date)
FROM synthea_cdm_plato.condition_occurrence
WHERE condition_concept_id = 4329847
AND condition_occurrence_id > 1000000;
"
```

### Step 2: Check Event Timing Relative to Index Date

```bash
# Confirm days_after_index for injected events (must be <= 30 for detection)
docker exec broadsea-atlasdb psql -U postgres -d postgres -c "
SELECT c.subject_id, c.cohort_start_date,
       co.condition_start_date,
       (co.condition_start_date - c.cohort_start_date) AS days_after_index
FROM plato_results.cohort c
JOIN synthea_cdm_plato.condition_occurrence co ON c.subject_id = co.person_id
WHERE c.cohort_definition_id = 941
AND co.condition_concept_id = 4329847
ORDER BY days_after_index
LIMIT 10;
"
```

If `days_after_index > 30`, re-inject with `index_date + 15` and delete the prior injected rows first.

### Step 3: Clear WebAPI Cache Before Re-Running

```bash
docker exec broadsea-atlasdb psql -U postgres -d postgres -c "
DELETE FROM webapi.generation_cache WHERE type='COHORT'
AND source_id IN (
  SELECT source_id FROM webapi.source
  WHERE source_key IN ('PLATO_BENCHMARK','ARISTOTLE_BENCHMARK','LEADER_BENCHMARK')
);
"
```

### Step 4: Re-Run 30-Day Analysis

```bash
# PLATO
curl -s -X POST "http://localhost/artemis-api/tte/studies/432/run-analysis" \
  -H "Content-Type: application/json" \
  -d '{"sourceKey":"PLATO_BENCHMARK"}'

# ARISTOTLE
curl -s -X POST "http://localhost/artemis-api/tte/studies/424/run-analysis" \
  -H "Content-Type: application/json" \
  -d '{"sourceKey":"ARISTOTLE_BENCHMARK"}'

# LEADER
curl -s -X POST "http://localhost/artemis-api/tte/studies/431/run-analysis" \
  -H "Content-Type: application/json" \
  -d '{"sourceKey":"LEADER_BENCHMARK"}'
```

---

## Benchmark Status

**Dataset**: `artemis/data/benchmark_data/ohdsi_criteria_benchmark.json` (8,210 items)

Domain distribution:
- Condition: 52%
- Drug: 25%
- Procedure: 11%
- Measurement: 8%

**Current Results (10-item sample — re-run required after bug fixes)**:

| Mapper | Recall | F1 | Latency | Notes |
|--------|--------|----|---------|-------|
| RAG (MedCPT + ChromaDB) | 0.056 | 0.026 | 1.7s | `top_k` fixed |
| LLM Direct (GPT-4o) | 0.161 | 0.061 | 1.7s | baseline |
| Agent2 (full pipeline) | 0.206 | 0.058 | 6.1s | `domain_hint` bug fixed |

**Action required**: Re-run full benchmark with `domain_hint` and dynamic `top_k` fixes applied (`d47f689`). Previous results do not reflect these fixes.

---

## Known Issues

| Issue | Location | Priority |
|-------|----------|----------|
| CV event injection date is 90 days — outside 30-day outcome window | DB injection script | **P0** — fix before next analysis run |
| PLATO `matchedPairs=1` — too few matched pairs for reliable PSM | PSM caliper tuning | P1 |
| `tte_store.py` missing concurrency guard | `tte_store.py` | P3 |
| MedCPT meta tensor initialization race condition | `artemis-api` startup | P2 |

---

## Infrastructure

| Item | Value |
|------|-------|
| Branch | `fix/agent1-pattern-e-or-logic` |
| Active container | `477b3f1664ed_artemis-api` |
| DB backups | `artemis/data/backups/synthea_cdm_{leader,plato,aristotle}_20260331.sql` |
| WebAPI data sources | `LEADER_BENCHMARK`, `PLATO_BENCHMARK`, `ARISTOTLE_BENCHMARK` |

---

## Key File References

- `artemis/src/services/tte_service.py` — `is_derived_rest` guard (line 4453), Agent5 error path
- `artemis/src/agents/agent5/workflow.py` — PSM→IPTW auto-fallback (line 206)
- `artemis/src/utils/omop_connector.py` — comparator cap, `followup_days` fix
- `scripts/quick_concept_benchmark.py` — `domain_hint` + `top_k` bug fixes
- `docs/tte_agent/06_backend_atomic_todo_plan.md` — active backend todo plan
- `docs/tte_agent/07_current_status.md` — current TTE module status
