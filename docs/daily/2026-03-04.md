# 2026-03-04

## Agent1 Exclusion → ABSENCE 자동 태깅 (00:10 완료)

### 변경 사항

- `parser.py`: `_build_criteria(force_logic_type)` 파라미터 추가, `_build_cohort_definition()`에서 exclusion_rules에 `force_logic_type="ABSENCE"` 강제 적용
- `parser.py`: flat-list fallback에서 `logic_type` 대소문자 정규화 (`.upper().strip()`)
- `prompts.py`: NCT_DECOMPOSITION_PROMPT Rule #2에 CRITICAL ABSENCE 지침 추가
- `test_agent1.py`: `TestExclusionPolarityEnforcement` 클래스 (2 tests) 추가

### 결과

- 테스트: 7/7 passed ✅
- 벤치마크(Exp D): F1 32.9% → 35.6% (+2.7pp), Precision +8.0pp
- 프롬프트 변경으로 Agent1 캐시 재생성됨 (새 hash)

### 💡 인사이트

- Agent3는 이미 `is_exclusion=True` → ABSENCE 처리 → 프로덕션 안전
- 문제는 benchmark 스크립트가 `rule.logic_type` 직접 읽는 구조
- 후처리 guardrail + 프롬프트 보강 = 이중 방어벽

---

## RFC-011 A: Agent2 캐시 통합 (00:35 완료)

### 변경 사항

- `benchmark_exp_d.py`: `Agent2Cache` 통합 — cache-aware `_invoke_agent2_with_split()`, batch flush, `[cached]` 태그
- `benchmark_exp_d.py`: `--no-agent2-cache` CLI 플래그, `cache_stats`/`timings` 리포트 JSON 추가
- `benchmark_a_direct.py`: 동일 캐시 패턴 적용

### 결과

| Run      | Cache Hit         | Agent2 Time | Phase 2 Total |
| -------- | ----------------- | ----------- | ------------- |
| Cold     | 58% (15H/11M)     | 189s        | 241.7s        |
| **Warm** | **100%** (26H/0M) | **3.0s**    | **61.3s**     |

- Warm run: Agent2 호출 완전 생략, Phase 2 **75% 단축**
- Cold/Warm 메트릭 동일 — 캐시 정합성 확인

### 💡 인사이트

- `put_batch()` single I/O로 per-rule 캐시 쓰기 최적화
- Exp D + A_direct 같은 캐시 파일 공유 → 상호 캐시 HIT 가능

---

## ADR-020: Main Paper Skip (00:50 완료)

### 변경 사항

- `parser.py` L118-129: supplement/appendix PDF 존재 시 main paper enrichment 건너뜀
- 의사결정: Appendix "Key Inclusion/Exclusion Criteria"가 complete → main paper는 noise만 추가
- [ADR-020](../adr/ADR-020_Skip_Main_Paper_With_Supplement.md) 작성

### 결과

- Input criteria: 173 (90%+ noise) → **23** (clean)
- Agent1 output: 19 rules → **31 rules** (LLM 재호출)
- "Acute decompensation of glycemic control" 생성됨 ✅
- 그러나 Insulin entity_text 폭발 → Precision 급락 (39.8% → 26.0%)

### 💡 인사이트

- Main paper `_extract_eligibility_section()` 실패 시 52K chars 전체 투입 = noise 원흉
- Appendix 2295 chars에 전체 eligibility criteria 포함 확인 (calcitonin~MTC 모두 있음)
- Codex CLI 심층 분석: 대안 B+조건부A 추천 → 채택

---

## Lab Meeting: Precision Recovery (14:10 완료)

### 참여 모델

Claude + Codex (gpt-5.3-codex-spark) / Gemini 2회 실패 → 제외

### 변경 사항

- `prompts.py` Rule #11: Drug entity_text specificity 지침 (generic, no few-shot)
- `parser.py`: `_normalize_drug_entity_text()` guardrail 추가 (Codex 구현)
  - Insulin/anti-diabetic broad 표현 → canonical specific form으로 rule-based 정규화

### 결과

| 메트릭           | main skip only | + guardrail        | 기존 best |
| ---------------- | -------------- | ------------------ | --------- |
| Recall           | 55.4%          | **54.1%**          | 51.5%     |
| **Precision**    | 26.0%          | **34.0% (+8pp)**   | 39.8%     |
| **F1**           | 28.6%          | **37.7% (+9.1pp)** | 37.8%     |
| Insulin resolved | 138,964        | **6,258**          | 10,638    |

- F1 37.7% — 기존 best(37.8%)와 동일 수준 회복
- Recall은 기존 대비 +2.6pp 유지

### 💡 인사이트

- LLM 비결정성은 프롬프트만으로 해결 불가 → rule-based guardrail 필요
- direct_map 하드코딩은 LEADER 한정이지만 fallback 패턴으로 일반화 보조
- [회의록](../lab_meetings/2026-03-04_exp_d_precision_recovery.md) 참조

---

## Lab Meeting: TROY vs SUPP Evaluation (19:00 완료)

### 참여 모델

Claude + Codex (gpt-5.3-codex-spark) / Gemini 2회 실패 → 제외

### 배경

- `LEADER_GOLD.json`: TROY 전문가 약칭 (concept set name: `"ACS"`, `"DPP4 inhibitors"`)
- `LEADER_GOLD_SUPP.json`: supplement appendix 원문 (concept set name: `"Acute coronary syndrome"`, `"DPP-4 inhibitor"`)
- 평가 목적: Agent2에 어떤 입력이 유리한지, 각 Agent 기여도 분해

### 변경 사항

- `LEADER_GOLD_SUPP.json`: 56개 concept set name + 18개 rule name을 supplement 원문으로 교체
- `workflow.py`: `FORCE_FAST_PATH` 환경변수 추가 (fast path 강제)
- `agent1_troy_mapping_supp.json`: SUPP용 explicit mapping 생성 (사용 안 함, 참고용)

### 합의

SUPP를 primary, TROY를 secondary로 사용. 둘 다 보고.

### 결과

| 조건              |  Recall   | Precision |    F1     |  Full  |
| ----------------- | :-------: | :-------: | :-------: | :----: |
| **Exp A (TROY)**  | **75.0%** |   56.6%   | **53.5%** | **11** |
| Exp A (SUPP)      |   60.4%   |   55.3%   |   45.4%   |   7    |
| Exp A (SUPP fast) |   55.3%   |   56.0%   |   41.4%   |   6    |

### 💡 인사이트

- **전문가 용어 우위**: TROY 약칭이 OMOP vocabulary에 14.6pp Recall 유리
- **Precision 안정**: 3조건 모두 55~56% → Agent2 false positive은 입력 무관
- **Slow path 기여 제한**: UMLS+reranking이 5.1pp만 추가 → 용어 표준화가 더 큰 레버
- [회의록](../lab_meetings/2026-03-04_troy_vs_supp_evaluation.md) 참조

---

## Codex CLI 리뷰

- Agent1 polarity: pre-review(구조 분석) + post-review(flat-list fallback 발견 → 수정)
- RFC-011 A: pre-review(raw-only 캐시 권장) + post-review(4검증 통과, no bugs)
- Main paper skip: Codex 심층 분석 → 대안 B+조건부A 추천
- Precision recovery: Codex가 직접 `_normalize_drug_entity_text()` 구현 + 프롬프트 Rule #11 추가
- LEADER_GOLD_SUPP: 6개 이슈 지적 (substring FP, trailing space, 괄호 과도 제거 등)

---

## 📋 다음 단계 (우선순위순)

### 🔴 높음

- [ ] No calcitonin / No acute decomp. EMPTY 해결 (LLM 비결정성 or TROY overlap 0)
- [ ] overlap density metric 실험 (Precision 추가 개선)
- [ ] RFC-010: Reranker Cross-Branch Recall 개선
- [ ] OMOP embedding 기반 direct mapping 탐색 (TROY vs SUPP gap 14.6pp 해소)

### 🟡 중간

- [ ] RFC-011 B: Agent2 호출 병렬화 + 중복 제거
- [ ] RFC-011 C: TROY resolve 캐시
- [ ] N:1 Matching 알고리즘 개선 (polarity-aware)
- [ ] 논문에 3실험(TROY/SUPP/E2E) 비교 보고

### 🟢 낮음

- [ ] dead code 정리 (`_normalize_drug_entity_text`의 `rule_name` unused param)
- [ ] RFC-011 E: resolve_troy_rule 배치화
- [ ] GOLD JSON 중복 ConceptSet 정리
